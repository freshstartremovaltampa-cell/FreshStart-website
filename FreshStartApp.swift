import SwiftUI

// Uncomment after adding the Firebase SDK:
// import FirebaseCore

@main
struct FreshStartApp: App {
    init() {
        // FirebaseApp.configure()
    }

    var body: some Scene {
        WindowGroup {
            MainDashboardView()
        }
    }
}
